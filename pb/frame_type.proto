syntax = "proto3";
option go_package = ".;protobuf";
package proto;

enum FrameType {

    // control frame
    OPEN = 0;
    HEARTBEAT = 1;
    CLOSE = 2;
    MAINTAIN = 3;

    // game message
    MATCHING = 10;
    MATCHINGCONFIRM = 11;
    MATCHINGERROR = 12;
    MATCHED = 13;
    MATCHEDCONFIRM = 14;


    // DROP_DOT = 5;
    // DROP_DOT_CONFIRM = 6;
    // DROP_CHECK = 7;
    // DROP_CHECK_CONFIRM = 8;
    // WIN = 9;
    // SURRENDER = 10;
}
